extends layout

block content
  h1= title
  
  -var image_names = ["meat1.jpeg", "meat2.jpg", "fruit1.jpg", "fruit2.jpeg", "veggie.jpg"]
  -for(let i =0; i<3; i++){
    img(src=`/images/${image_names[Math.floor(Math.random()*5)]}`, alt=300, height=300, width=300)
    -}

