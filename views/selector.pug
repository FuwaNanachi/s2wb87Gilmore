extends layout

block content
  h1= title
  
  -var image_names = ["meat1", "meat2", "fruit1", "fruit2", "veggie"]
  -for(let i =0; i<3; i++){
    img(src=`/images/${Math.floor(Math.random()*5)}`, alt="300", height="300", width="300")
    -}

