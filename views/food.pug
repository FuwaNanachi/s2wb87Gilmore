extends layout

block content
  h1= title
  -let item= results
  div(class = "container-fluid")
    div(class="row")
      each item in results 
        div(class = "col-sm-2") #{item.category} Category #{item.quality} Quality #{item.cost} Cost
  

      

